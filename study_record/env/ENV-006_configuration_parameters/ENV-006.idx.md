# ENV-006 配置文件与参数体系（生产向 P0）

- 任务 ID：`ENV-006`
- 系列状态：`✅ 已完成（v1.1，2026-09-01）`
- 权重：`M`（生产向 P0：敢登录、敢看、不会误操作）
- 首次开始日期：`2026-08-28`
- 分类：`env/configuration`
- 关联：learning-roadmap.md ENV-006；pg-mysql-map.md（pg_settings/SET PERSIST 行）；runbook cheatsheet 参数段

## 研究目标

以 PG 18.4 的 postgresql.conf / ALTER SYSTEM / pg_settings(context) / reload vs restart 心智为基线，回答：
MySQL 配置从哪些文件来、优先级如何、GLOBAL/SESSION/PERSIST/PERSIST_ONLY 四种修改方式、
哪些参数在线可改/只影响新连接/必须重启、PERSIST 重启后是否保留。

## 完成清单

- [x] PG 基线：pg_settings context 分层（postmaster/sighup/superuser/user）、ALTER SYSTEM+reload、pending_restart=t、session SET
- [x] MySQL 配置搜索路径（mysqld --verbose --help）与 my_print_defaults 实测
- [x] SHOW VARIABLES 三式（GLOBAL/SESSION/@@）
- [x] SESSION 级修改（transaction_isolation、sort_buffer_size，新会话不回显）
- [x] 动态 GLOBAL（max_connections 200→100；server_id 8.0+ 在线可改）
- [x] 静态参数报错 ERROR 1238（port/performance_schema）
- [x] SET PERSIST + 受控重启验证（max_connections 105 重启后保留）
- [x] SET PERSIST_ONLY（innodb_buffer_pool_size 只写文件不生效）+ RESET PERSIST
- [x] 实验状态全部还原（100/1/64M/auto.cnf 空）
- [x] 对照文章 + roadmap/map/runbook 更新
- [x] 理解验证（2026-09-01 实机自测 8 步全过，evidence/selfcheck-variables_info.txt）
- [x] v1.1（2026-09-01）：修正"P_S=OFF 时 variables_info 不可用"旧结论（实测 setup 表仍可查，来源/路径/修改人全有）

## 关键证据

- evidence/pg-config.txt：PG 侧 context/ALTER SYSTEM/pending_restart/还原全记录
- evidence/mysql-config.txt：MySQL 侧搜索路径/GLOBAL/SESSION/PERSIST/1238/重启验证全记录
- evidence/selfcheck-variables_info.txt：2026-09-01 理解验证自测 8 步 + variables_info 可用性修正证据

## 关键结论（摘要）

1. MySQL 配置来源链：命令行 > mysqld-auto.cnf（SET PERSIST）> my.cnf 搜索路径；本机只有 /etc/my.cnf。
2. 修改方式四态：SET SESSION（本会话）/ SET GLOBAL（在线，新连接生效）/ SET PERSIST（在线+写 auto.cnf，重启保留）/ SET PERSIST_ONLY（只写文件）。
3. 静态参数（port/performance_schema/...）SET GLOBAL 报 ERROR 1238 read only；server_id 在 8.0+ 已变为动态。
4. PERSIST 重启验证通过：my.cnf=100 → SET PERSIST 105 → 重启后 105（auto.cnf 优先级高于 my.cnf）。
5. PG 对照：pg_settings.context 决定改法（postmaster 需重启 pending_restart=t；superuser/sighup reload 即生效）；MySQL 用"是否报 1238"判断静态，无 pending_restart 等价物（PERSIST_ONLY 是近似）。
6. v1.1 修正：P_S=OFF 只禁用 instrumentation 事件表；variables_info/global_variables 是 setup 表仍可查，
   VARIABLE_SOURCE（COMPILED/GLOBAL/COMMAND_LINE/DYNAMIC/PERSISTED）+ VARIABLE_PATH + SET_TIME/SET_USER 即"参数来源"答案；
   information_schema.global_variables 在 8.4 已移除（08-28 结论"来源只能靠 my_print_defaults+auto.cnf"不准确）。

## 后续

- ENV-007 登录与认证（socket/TCP/host 匹配/认证插件/login-path）
- MON-001 慢日志/P_S（受 P_S=OFF 约束）
